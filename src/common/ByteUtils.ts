export function deserializeUpToInt64BigEndian(data: Buffer, offset: number,
        length: number, signed: boolean): bigint {
    return 0n
}

export function serializeUpToInt64BigEndian(v: bigint, data: Buffer,
        offset: number, length: number) {

}
